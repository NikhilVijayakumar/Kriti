id: experimental-setup
domain: experimental-setup
checks:
  - id: es-001
    name: word_count_in_range
    rule: word_count_in_range
    severity: critical
    config: { min: 400, max: 1000 }
    description: "Experimental setup word count within configured range"
  - id: es-002
    name: hardware_specified
    rule: regex_match
    severity: warning
    config: { pattern: "(?i)(?:GPU|CPU|RAM|hardware|server|machine)" }
    description: "Hardware specification present"
  - id: es-003
    name: software_versions
    rule: regex_match
    severity: warning
    config: { pattern: "(?i)(?:version|v\\d+\\.\\d+|\\d+\\.\\d+)" }
    description: "Software versions listed"
  - id: es-004
    name: dataset_described
    rule: regex_match
    severity: warning
    config: { pattern: "(?i)(?:dataset|corpus|data\\s+source|preprocessing)" }
    description: "Dataset source and preprocessing described"
  - id: es-005
    name: min_diagram_count
    rule: min_diagram_count
    severity: warning
    config: { min: 1 }
    description: "At least one diagram present (architecture, pipeline, etc.)"
  - id: es-006
    name: no_placeholders
    rule: no_placeholders
    severity: critical
    description: "No unfilled template markers"
  - id: exps-cit
    name: citation_marker_present
    rule: min_citation_count
    severity: critical
    config: { min: 1 }
    description: "Domain contains at least one citation marker"
  - id: exps-bfit
    name: budget_fit_applied
    rule: budget_fit_applied
    severity: critical
    description: "stage='budget-fit' narrative row exists for this domain"
