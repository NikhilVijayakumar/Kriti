# 07. Runtime Standards

**Domain:** Runtime
**Audit Target:** `main.py`, `app.py`, `run.py`

## Standard Definition
The final execution artifact must be clearly identifiable to ensure smooth deployment and testing by the judging panel.

### Expected Evidence
1. **Entrypoint:** The root directory must contain an obvious execution script (e.g., `main.py`).
