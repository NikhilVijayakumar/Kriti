id: deterministic_08_team-workflow
calculation: weighted_pass_rate
scope: document
inputs:
  from: audit/deterministic/document/08-team-workflow.yaml
  fields: [id, weight, mandatory, passed]
formula: |
  score = 100 * (sum(weight) where passed=true) / (sum(weight), all rules)
rollup: null
note: Specific deterministic calculation for team-workflow.
