# System class definition for react_dev
# Inherits the full base_dev domain set — no drops needed.
name: react_dev
extends: base_dev
description: >
  React frontend development system. Inherits all 16 domains from
  base_dev. Adds React-specific templates and component patterns.
