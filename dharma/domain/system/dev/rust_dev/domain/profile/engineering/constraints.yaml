schema:
  id: rust_dev.dharma.domain.engineering.profile.constraints
  name: Constraints Section Profile
  version: 0.1.0
source: >-
  common/tier2/audit/deterministic/section/engineering/07-constraints.yaml
section:
  id: constraints
  writing_objective: >-
    Authored placeholder - the rust_dev standard's common/tier2/audit/deterministic/section/engineering/07-constraints.yaml
    carries no Writing Guidance for this section; objective pending human author.
    Section behavior is defined by its 3 deterministic audit rule(s) (see validation.rules).
  knowledge_goal: >-
    Authored placeholder - no source exists in the rust_dev standard for this field;
    generated by generate_domain_map_profile.py until a human author writes it.
  reader_goal: >-
    Authored placeholder - no source exists in the rust_dev standard for this field;
    generated by generate_domain_map_profile.py until a human author writes it.
  required_inputs: []
  expected_outputs:
    primary: []
  subsections: {}
  completion:
    checklist:
    - Constraints section exists
    - Constraints section defines engineering constraints
    - Constraints have clear sources
  review:
    questions: []
  validation:
    rules:
    - id: eng-sec-constraints-001
      description: Constraints section exists
      condition: document has a section with semantic_type = 'constraints'
      message: Missing optional 'Constraints' section
      severity: info
      weight: 0.3
      mandatory: false
      evidence:
        type: section_presence
        semantic_type: constraints
    - id: eng-sec-constraints-002
      description: Constraints section defines engineering constraints
      condition: >-
        section contains specific constraints on engineering practices such as regulatory,
        operational, or technical constraints
      message: Constraints section does not define engineering constraints
      severity: warning
      weight: 0.5
      mandatory: false
      evidence:
        type: content_check
        keywords:
        - must
        - shall
        - cannot
        - prohibited
        - constraint
        - limitation
    - id: eng-sec-constraints-003
      description: Constraints have clear sources
      condition: >-
        each constraint identifies where it originates (regulation, architecture,
        business)
      message: Constraints section does not identify constraint sources
      severity: warning
      weight: 0.5
      mandatory: false
      evidence:
        type: content_check
        keywords:
        - regulation
        - compliance
        - architecture
        - business
        - regulatory
