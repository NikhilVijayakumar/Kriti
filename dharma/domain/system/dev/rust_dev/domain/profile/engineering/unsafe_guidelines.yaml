schema:
  id: rust_dev.dharma.domain.engineering.profile.unsafe_guidelines
  name: Unsafe Guidelines Section Profile
  version: 0.1.0
source: >-
  common/tier2/audit/deterministic/section/engineering/11-unsafe_guidelines.yaml
section:
  id: unsafe_guidelines
  writing_objective: >-
    Write the Unsafe Guidelines section, in the third person voice, in paragraphs
    form, with a strict tone, for the systems engineer. Do: Enforce `#![forbid(unsafe_code)]`
    by default.. Don't: Leave the `unsafe` policy ambiguous..
  knowledge_goal: >-
    Authored placeholder - no source exists in the rust_dev standard for this field;
    generated by generate_domain_map_profile.py until a human author writes it.
  reader_goal: >-
    Authored placeholder - no source exists in the rust_dev standard for this field;
    generated by generate_domain_map_profile.py until a human author writes it.
  required_inputs: []
  expected_outputs:
    primary:
    - at least 1 paragraph(s)
  subsections: {}
  completion:
    checklist:
    - 'Template conformance: Unsafe Guidelines'
  review:
    questions: []
  validation:
    rules:
    - id: eng-sec-unsafe-guidelines-001
      description: 'Template conformance: Unsafe Guidelines'
      condition: section structurally matches the template requirements
      message: Section fails structural validation
      severity: error
      weight: 2.0
      mandatory: true
      evidence:
        type: structural_check
        min_paragraphs: 1
