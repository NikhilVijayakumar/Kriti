schema:
  id: rust_dev.dharma.domain.engineering.profile.traceability
  name: Traceability Section Profile
  version: 0.1.0
source: >-
  common/tier2/audit/deterministic/section/engineering/08-traceability.yaml
section:
  id: traceability
  writing_objective: >-
    Authored placeholder - the rust_dev standard's common/tier2/audit/deterministic/section/engineering/08-traceability.yaml
    carries no Writing Guidance for this section; objective pending human author.
    Section behavior is defined by its 3 deterministic audit rule(s) (see validation.rules).
  knowledge_goal: >-
    Authored placeholder - no source exists in the rust_dev standard for this field;
    generated by generate_domain_map_profile.py until a human author writes it.
  reader_goal: >-
    Authored placeholder - no source exists in the rust_dev standard for this field;
    generated by generate_domain_map_profile.py until a human author writes it.
  required_inputs: []
  expected_outputs:
    primary: []
  subsections: {}
  completion:
    checklist:
    - Traceability section exists
    - Traceability links to upstream domains
    - Traceability links to downstream implementation
  review:
    questions: []
  validation:
    rules:
    - id: eng-sec-trace-001
      description: Traceability section exists
      condition: document has a section with semantic_type = 'traceability'
      message: Missing optional 'Traceability' section
      severity: info
      weight: 0.3
      mandatory: false
      evidence:
        type: section_presence
        semantic_type: traceability
    - id: eng-sec-trace-002
      description: Traceability links to upstream domains
      condition: >-
        section contains references back to Philosophy, Architecture, or Vision that
        informed the engineering standards
      message: Traceability section does not link to upstream domains
      severity: warning
      weight: 0.5
      mandatory: false
      evidence:
        type: content_check
        keywords:
        - derived from
        - informed by
        - traceable
        - links to
    - id: eng-sec-trace-003
      description: Traceability links to downstream implementation
      condition: >-
        section contains forward references to Implementation or Feature Technical
        that consume these standards
      message: >-
        Traceability section does not link to downstream implementation
      severity: warning
      weight: 0.5
      mandatory: false
      evidence:
        type: content_check
        keywords:
        - implemented by
        - consumed by
        - realized in
        - applied to
