schema:
  id: rust_dev.dharma.domain.external-context.profile.constraints
  name: Constraints Section Profile
  version: 0.1.0
source: >-
  common/tier2/audit/deterministic/section/external-context/03-constraints.yaml
section:
  id: constraints
  writing_objective: >-
    Authored placeholder - the rust_dev standard's common/tier2/audit/deterministic/section/external-context/03-constraints.yaml
    carries no Writing Guidance for this section; objective pending human author.
    Section behavior is defined by its 3 deterministic audit rule(s) (see validation.rules).
  knowledge_goal: >-
    Authored placeholder - no source exists in the rust_dev standard for this field;
    generated by generate_domain_map_profile.py until a human author writes it.
  reader_goal: >-
    Authored placeholder - no source exists in the rust_dev standard for this field;
    generated by generate_domain_map_profile.py until a human author writes it.
  required_inputs: []
  expected_outputs:
    primary: []
  subsections: {}
  completion:
    checklist:
    - Constraints section exists
    - Constraints are clearly stated
    - Constraints trace to external realities
  review:
    questions: []
  validation:
    rules:
    - id: ext-sec-con-001
      description: Constraints section exists
      condition: document has a section with semantic_type = 'constraints'
      message: Missing 'Constraints' section
      severity: warning
      weight: 0.5
      mandatory: false
      evidence:
        type: section_presence
        semantic_type: constraints
    - id: ext-sec-con-002
      description: Constraints are clearly stated
      condition: each constraint is a discrete, unambiguous restriction
      message: Constraints are ambiguous or unclear
      severity: warning
      weight: 0.5
      mandatory: false
      evidence:
        type: content_check
        patterns:
        - shall not
        - must not
        - limited to
        - maximum
        - minimum
    - id: ext-sec-con-003
      description: Constraints trace to external realities
      condition: >-
        constraints reference real-world restrictions (regulations, vendor limits,
        physical constraints)
      message: Constraints do not trace to external realities
      severity: warning
      weight: 0.5
      mandatory: false
      evidence:
        type: content_check
        keywords:
        - regulation
        - compliance
        - vendor
        - physical
        - legal
