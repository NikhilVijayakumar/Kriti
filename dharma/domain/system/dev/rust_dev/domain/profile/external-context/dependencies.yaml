schema:
  id: rust_dev.dharma.domain.external-context.profile.dependencies
  name: Dependencies Section Profile
  version: 0.1.0
source: >-
  common/tier2/audit/deterministic/section/external-context/04-dependencies.yaml
section:
  id: dependencies
  writing_objective: >-
    Authored placeholder - the rust_dev standard's common/tier2/audit/deterministic/section/external-context/04-dependencies.yaml
    carries no Writing Guidance for this section; objective pending human author.
    Section behavior is defined by its 3 deterministic audit rule(s) (see validation.rules).
  knowledge_goal: >-
    Authored placeholder - no source exists in the rust_dev standard for this field;
    generated by generate_domain_map_profile.py until a human author writes it.
  reader_goal: >-
    Authored placeholder - no source exists in the rust_dev standard for this field;
    generated by generate_domain_map_profile.py until a human author writes it.
  required_inputs: []
  expected_outputs:
    primary: []
  subsections: {}
  completion:
    checklist:
    - Dependencies section exists
    - Dependencies are enumerated
    - Dependencies distinguish direction
  review:
    questions: []
  validation:
    rules:
    - id: ext-sec-dep-001
      description: Dependencies section exists
      condition: document has a section with semantic_type = 'dependencies'
      message: Missing 'Dependencies' section
      severity: warning
      weight: 0.5
      mandatory: false
      evidence:
        type: section_presence
        semantic_type: dependencies
    - id: ext-sec-dep-002
      description: Dependencies are enumerated
      condition: >-
        section lists each dependency on external systems, services, or standards
      message: Dependencies are not clearly enumerated
      severity: warning
      weight: 0.5
      mandatory: false
      evidence:
        type: content_check
        min_count: 1
    - id: ext-sec-dep-003
      description: Dependencies distinguish direction
      condition: >-
        each dependency states whether it is inbound (external depends on us) or outbound
        (we depend on external)
      message: Dependencies do not distinguish direction
      severity: warning
      weight: 0.5
      mandatory: false
      evidence:
        type: content_check
        keywords:
        - inbound
        - outbound
        - we depend
        - depends on us
        - consumer
        - provider
