schema:
  id: rust_dev.dharma.domain.readme.profile.repository_structure
  name: Repository Structure Section Profile
  version: 0.1.0
source: >-
  common/tier8/audit/deterministic/section/readme/06-repository_structure.yaml
section:
  id: repository_structure
  writing_objective: >-
    Authored placeholder - the rust_dev standard's common/tier8/audit/deterministic/section/readme/06-repository_structure.yaml
    carries no Writing Guidance for this section; objective pending human author.
    Section behavior is defined by its 3 deterministic audit rule(s) (see validation.rules).
  knowledge_goal: >-
    Authored placeholder - no source exists in the rust_dev standard for this field;
    generated by generate_domain_map_profile.py until a human author writes it.
  reader_goal: >-
    Authored placeholder - no source exists in the rust_dev standard for this field;
    generated by generate_domain_map_profile.py until a human author writes it.
  required_inputs: []
  expected_outputs:
    primary: []
  subsections: {}
  completion:
    checklist:
    - Repository structure section exists
    - Repository structure describes layout
    - Repository structure lists key directories
  review:
    questions: []
  validation:
    rules:
    - id: readme-sec-repo-001
      description: Repository structure section exists
      condition: >-
        document has a section with semantic_type = 'repository_structure'
      message: Missing required 'Repository Structure' section
      severity: error
      weight: 1.5
      mandatory: true
      evidence:
        type: section_presence
        semantic_type: repository_structure
    - id: readme-sec-repo-002
      description: Repository structure describes layout
      condition: >-
        section contains a description of the repository directory layout
      message: Repository structure section does not describe layout
      severity: error
      weight: 1.0
      mandatory: true
      evidence:
        type: content_check
        patterns:
        - directory
        - folder
        - structure
        - layout
    - id: readme-sec-repo-003
      description: Repository structure lists key directories
      condition: section lists at least three key directories or files
      message: Repository structure does not list enough directories
      severity: warning
      weight: 0.5
      mandatory: false
      evidence:
        type: content_check
        patterns:
        - src
        - docs
        - test
        - lib
