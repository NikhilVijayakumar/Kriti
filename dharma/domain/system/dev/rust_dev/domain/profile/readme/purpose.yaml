schema:
  id: rust_dev.dharma.domain.readme.profile.purpose
  name: Purpose Section Profile
  version: 0.1.0
source: >-
  common/tier8/audit/deterministic/section/readme/04-purpose.yaml
section:
  id: purpose
  writing_objective: >-
    Authored placeholder - the rust_dev standard's common/tier8/audit/deterministic/section/readme/04-purpose.yaml
    carries no Writing Guidance for this section; objective pending human author.
    Section behavior is defined by its 3 deterministic audit rule(s) (see validation.rules).
  knowledge_goal: >-
    Authored placeholder - no source exists in the rust_dev standard for this field;
    generated by generate_domain_map_profile.py until a human author writes it.
  reader_goal: >-
    Authored placeholder - no source exists in the rust_dev standard for this field;
    generated by generate_domain_map_profile.py until a human author writes it.
  required_inputs: []
  expected_outputs:
    primary: []
  subsections: {}
  completion:
    checklist:
    - Purpose section exists
    - Purpose states project intent
    - Purpose defines scope boundaries
  review:
    questions: []
  validation:
    rules:
    - id: readme-sec-purpose-001
      description: Purpose section exists
      condition: document has a section with semantic_type = 'purpose'
      message: Missing required 'Purpose' section
      severity: error
      weight: 1.5
      mandatory: true
      evidence:
        type: section_presence
        semantic_type: purpose
    - id: readme-sec-purpose-002
      description: Purpose states project intent
      condition: section contains a statement of why the project exists
      message: Purpose section does not state project intent
      severity: error
      weight: 1.0
      mandatory: true
      evidence:
        type: content_check
        keywords:
        - purpose
        - intent
        - why
        - exists
    - id: readme-sec-purpose-003
      description: Purpose defines scope boundaries
      condition: section defines what the project is and is not
      message: Purpose section does not define scope boundaries
      severity: warning
      weight: 0.5
      mandatory: false
      evidence:
        type: content_check
        patterns:
        - unlike
        - not
        - does not
