schema:
  id: rust_dev.dharma.domain.readme.profile.configuration
  name: Configuration Section Profile
  version: 0.1.0
source: >-
  common/tier8/audit/deterministic/section/readme/14-configuration.yaml
section:
  id: configuration
  writing_objective: >-
    Authored placeholder - the rust_dev standard's common/tier8/audit/deterministic/section/readme/14-configuration.yaml
    carries no Writing Guidance for this section; objective pending human author.
    Section behavior is defined by its 3 deterministic audit rule(s) (see validation.rules).
  knowledge_goal: >-
    Authored placeholder - no source exists in the rust_dev standard for this field;
    generated by generate_domain_map_profile.py until a human author writes it.
  reader_goal: >-
    Authored placeholder - no source exists in the rust_dev standard for this field;
    generated by generate_domain_map_profile.py until a human author writes it.
  required_inputs: []
  expected_outputs:
    primary: []
  subsections: {}
  completion:
    checklist:
    - Configuration section exists
    - Configuration has substantive content
    - Configuration is specific to this project
  review:
    questions: []
  validation:
    rules:
    - id: readme-sec-configuration-001
      description: Configuration section exists
      condition: document has a section with semantic_type = 'configuration'
      message: Missing required 'Configuration' section
      severity: error
      weight: 1.5
      mandatory: true
      evidence:
        type: section_presence
        semantic_type: configuration
    - id: readme-sec-configuration-002
      description: Configuration has substantive content
      condition: >-
        section contains at least one paragraph of project-specific content
      message: Configuration section is empty or placeholder only
      severity: error
      weight: 1.0
      mandatory: true
      evidence:
        type: content_check
        min_length: 100
    - id: readme-sec-configuration-003
      description: Configuration is specific to this project
      condition: >-
        section contains project-specific details, not generic boilerplate
      message: Configuration contains generic content
      severity: warning
      weight: 0.5
      mandatory: false
      evidence:
        type: content_check
        keywords:
        - readme
