schema:
  id: rust_dev.dharma.domain.readme.profile.build
  name: Build Section Profile
  version: 0.1.0
source: >-
  common/tier8/audit/deterministic/section/readme/10-build.yaml
section:
  id: build
  writing_objective: >-
    Authored placeholder - the rust_dev standard's common/tier8/audit/deterministic/section/readme/10-build.yaml
    carries no Writing Guidance for this section; objective pending human author.
    Section behavior is defined by its 3 deterministic audit rule(s) (see validation.rules).
  knowledge_goal: >-
    Authored placeholder - no source exists in the rust_dev standard for this field;
    generated by generate_domain_map_profile.py until a human author writes it.
  reader_goal: >-
    Authored placeholder - no source exists in the rust_dev standard for this field;
    generated by generate_domain_map_profile.py until a human author writes it.
  required_inputs: []
  expected_outputs:
    primary: []
  subsections: {}
  completion:
    checklist:
    - Build section exists
    - Build provides instructions
    - Build references build documentation
  review:
    questions: []
  validation:
    rules:
    - id: readme-sec-build-001
      description: Build section exists
      condition: document has a section with semantic_type = 'build'
      message: Missing required 'Build' section
      severity: error
      weight: 1.5
      mandatory: true
      evidence:
        type: section_presence
        semantic_type: build
    - id: readme-sec-build-002
      description: Build provides instructions
      condition: section contains build instructions or a pointer to them
      message: Build section does not provide build instructions
      severity: error
      weight: 1.0
      mandatory: true
      evidence:
        type: content_check
        keywords:
        - build
        - compile
        - make
        - run
    - id: readme-sec-build-003
      description: Build references build documentation
      condition: >-
        section references Build Documentation for detailed build process
      message: Build section does not reference Build Documentation
      severity: warning
      weight: 0.5
      mandatory: false
      evidence:
        type: cross_reference
        expected:
        - domain: build
          direction: derives_from
