schema:
  id: rust_dev.dharma.domain.readme.profile.usage
  name: Usage Section Profile
  version: 0.1.0
source: >-
  common/tier8/audit/deterministic/section/readme/11-usage.yaml
section:
  id: usage
  writing_objective: >-
    Authored placeholder - the rust_dev standard's common/tier8/audit/deterministic/section/readme/11-usage.yaml
    carries no Writing Guidance for this section; objective pending human author.
    Section behavior is defined by its 3 deterministic audit rule(s) (see validation.rules).
  knowledge_goal: >-
    Authored placeholder - no source exists in the rust_dev standard for this field;
    generated by generate_domain_map_profile.py until a human author writes it.
  reader_goal: >-
    Authored placeholder - no source exists in the rust_dev standard for this field;
    generated by generate_domain_map_profile.py until a human author writes it.
  required_inputs: []
  expected_outputs:
    primary: []
  subsections: {}
  completion:
    checklist:
    - Usage section exists
    - Usage provides examples
    - Usage references feature docs
  review:
    questions: []
  validation:
    rules:
    - id: readme-sec-usage-001
      description: Usage section exists
      condition: document has a section with semantic_type = 'usage'
      message: Missing required 'Usage' section
      severity: error
      weight: 1.5
      mandatory: true
      evidence:
        type: section_presence
        semantic_type: usage
    - id: readme-sec-usage-002
      description: Usage provides examples
      condition: section contains at least one usage example or code snippet
      message: Usage section has no examples
      severity: error
      weight: 1.0
      mandatory: true
      evidence:
        type: content_check
        patterns:
        - example
        - usage
        - '```'
        - code
    - id: readme-sec-usage-003
      description: Usage references feature docs
      condition: section references Feature Documentation for detailed usage
      message: Usage does not reference Feature Documentation
      severity: warning
      weight: 0.5
      mandatory: false
      evidence:
        type: cross_reference
        expected:
        - domain: feature
          direction: derives_from
