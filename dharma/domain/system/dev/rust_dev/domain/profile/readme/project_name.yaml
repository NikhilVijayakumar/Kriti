schema:
  id: rust_dev.dharma.domain.readme.profile.project_name
  name: Project Name Section Profile
  version: 0.1.0
source: >-
  common/tier8/audit/deterministic/section/readme/01-project_name.yaml
section:
  id: project_name
  writing_objective: >-
    Authored placeholder - the rust_dev standard's common/tier8/audit/deterministic/section/readme/01-project_name.yaml
    carries no Writing Guidance for this section; objective pending human author.
    Section behavior is defined by its 3 deterministic audit rule(s) (see validation.rules).
  knowledge_goal: >-
    Authored placeholder - no source exists in the rust_dev standard for this field;
    generated by generate_domain_map_profile.py until a human author writes it.
  reader_goal: >-
    Authored placeholder - no source exists in the rust_dev standard for this field;
    generated by generate_domain_map_profile.py until a human author writes it.
  required_inputs: []
  expected_outputs:
    primary: []
  subsections: {}
  completion:
    checklist:
    - Project name section exists
    - Project name is present
    - Project name matches repository
  review:
    questions: []
  validation:
    rules:
    - id: readme-sec-name-001
      description: Project name section exists
      condition: document has a section with semantic_type = 'project_name'
      message: Missing required 'Project Name' section
      severity: error
      weight: 1.5
      mandatory: true
      evidence:
        type: section_presence
        semantic_type: project_name
    - id: readme-sec-name-002
      description: Project name is present
      condition: section contains a non-empty project name
      message: Project name section is empty
      severity: error
      weight: 1.0
      mandatory: true
      evidence:
        type: content_check
        keywords:
        - project
        - name
    - id: readme-sec-name-003
      description: Project name matches repository
      condition: project name matches the repository or package name
      message: Project name does not match repository name
      severity: warning
      weight: 0.5
      mandatory: false
      evidence:
        type: content_check
        patterns:
        - samgraha
