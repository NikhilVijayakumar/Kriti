schema:
  id: rust_dev.dharma.domain.readme.profile.installation
  name: Installation Section Profile
  version: 0.1.0
source: >-
  common/tier8/audit/deterministic/section/readme/09-installation.yaml
section:
  id: installation
  writing_objective: >-
    Authored placeholder - the rust_dev standard's common/tier8/audit/deterministic/section/readme/09-installation.yaml
    carries no Writing Guidance for this section; objective pending human author.
    Section behavior is defined by its 3 deterministic audit rule(s) (see validation.rules).
  knowledge_goal: >-
    Authored placeholder - no source exists in the rust_dev standard for this field;
    generated by generate_domain_map_profile.py until a human author writes it.
  reader_goal: >-
    Authored placeholder - no source exists in the rust_dev standard for this field;
    generated by generate_domain_map_profile.py until a human author writes it.
  required_inputs: []
  expected_outputs:
    primary: []
  subsections: {}
  completion:
    checklist:
    - Installation section exists
    - Installation provides steps
    - Installation lists dependencies
  review:
    questions: []
  validation:
    rules:
    - id: readme-sec-install-001
      description: Installation section exists
      condition: document has a section with semantic_type = 'installation'
      message: Missing required 'Installation' section
      severity: error
      weight: 1.5
      mandatory: true
      evidence:
        type: section_presence
        semantic_type: installation
    - id: readme-sec-install-002
      description: Installation provides steps
      condition: section contains installation steps or a pointer to them
      message: Installation section does not provide installation steps
      severity: error
      weight: 1.0
      mandatory: true
      evidence:
        type: content_check
        keywords:
        - install
        - setup
        - configure
        - pip
        - npm
    - id: readme-sec-install-003
      description: Installation lists dependencies
      condition: section lists required dependencies or tools
      message: Installation does not list dependencies
      severity: warning
      weight: 0.5
      mandatory: false
      evidence:
        type: content_check
        patterns:
        - dependency
        - require
        - need
        - tool
