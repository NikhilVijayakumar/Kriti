schema:
  id: >-
    rust_dev.dharma.domain.implementation.profile.security_fix_plan
  name: Security Fix Plan Section Profile
  version: 0.1.0
source: >-
  common/tier5/audit/deterministic/section/implementation/02-security_fix_plan.yaml
section:
  id: security_fix_plan
  writing_objective: >-
    Authored placeholder - the rust_dev standard's common/tier5/audit/deterministic/section/implementation/02-security_fix_plan.yaml
    carries no Writing Guidance for this section; objective pending human author.
    Section behavior is defined by its 4 deterministic audit rule(s) (see validation.rules).
  knowledge_goal: >-
    Authored placeholder - no source exists in the rust_dev standard for this field;
    generated by generate_domain_map_profile.py until a human author writes it.
  reader_goal: >-
    Authored placeholder - no source exists in the rust_dev standard for this field;
    generated by generate_domain_map_profile.py until a human author writes it.
  required_inputs: []
  expected_outputs:
    primary: []
  subsections: {}
  completion:
    checklist:
    - Security fix plan section exists
    - Security fix plan defines fixes
    - Security fix plan references security standard
    - Security fix plan traces to threats
  review:
    questions: []
  validation:
    rules:
    - id: impl-sec-fix-001
      description: Security fix plan section exists
      condition: >-
        document has a section with semantic_type = 'security_fix_plan'
      message: Missing required 'Security Fix Plan' section
      severity: error
      weight: 1.5
      mandatory: true
      evidence:
        type: section_presence
        semantic_type: security_fix_plan
    - id: impl-sec-fix-002
      description: Security fix plan defines fixes
      condition: >-
        section contains at least one security fix or remediation step
      message: Security fix plan section has no fixes defined
      severity: error
      weight: 1.0
      mandatory: true
      evidence:
        type: content_check
        patterns:
        - fix
        - remediation
        - patch
        - mitigation
    - id: impl-sec-fix-003
      description: Security fix plan references security standard
      condition: >-
        section references Security Documentation threat model or mitigation strategies
      message: Security fix plan does not reference Security Documentation
      severity: warning
      weight: 0.5
      mandatory: false
      evidence:
        type: cross_reference
        expected:
        - domain: security
          direction: derives_from
    - id: impl-sec-fix-004
      description: Security fix plan traces to threats
      condition: >-
        each fix references the specific threat or vulnerability it addresses
      message: Security fixes do not trace to specific threats
      severity: warning
      weight: 0.5
      mandatory: false
      evidence:
        type: cross_reference
        expected:
        - domain: security
          direction: traceable_to
