schema:
  id: rust_dev.dharma.domain.implementation.profile.purpose
  name: Purpose Section Profile
  version: 0.1.0
source: >-
  common/tier5/audit/deterministic/section/implementation/03-purpose.yaml
section:
  id: purpose
  writing_objective: >-
    Authored placeholder - the rust_dev standard's common/tier5/audit/deterministic/section/implementation/03-purpose.yaml
    carries no Writing Guidance for this section; objective pending human author.
    Section behavior is defined by its 3 deterministic audit rule(s) (see validation.rules).
  knowledge_goal: >-
    Authored placeholder - no source exists in the rust_dev standard for this field;
    generated by generate_domain_map_profile.py until a human author writes it.
  reader_goal: >-
    Authored placeholder - no source exists in the rust_dev standard for this field;
    generated by generate_domain_map_profile.py until a human author writes it.
  required_inputs: []
  expected_outputs:
    primary: []
  subsections: {}
  completion:
    checklist:
    - Purpose section exists
    - Purpose states implementation intent
    - Purpose defines scope boundaries
  review:
    questions: []
  validation:
    rules:
    - id: impl-sec-purpose-001
      description: Purpose section exists
      condition: document has a section with semantic_type = 'purpose'
      message: Missing optional 'Purpose' section
      severity: warning
      weight: 0.5
      mandatory: false
      evidence:
        type: section_presence
        semantic_type: purpose
    - id: impl-sec-purpose-002
      description: Purpose states implementation intent
      condition: >-
        section contains a statement of why Implementation Documentation exists
      message: Purpose section does not state implementation intent
      severity: warning
      weight: 0.5
      mandatory: false
      evidence:
        type: content_check
        keywords:
        - implementation
        - generation
        - purpose
        - intent
    - id: impl-sec-purpose-003
      description: Purpose defines scope boundaries
      condition: >-
        section defines what Implementation Documentation is and is not
      message: Purpose section does not define scope boundaries
      severity: warning
      weight: 0.5
      mandatory: false
      evidence:
        type: content_check
        patterns:
        - unlike
        - not
        - does not
