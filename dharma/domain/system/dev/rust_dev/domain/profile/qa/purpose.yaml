schema:
  id: rust_dev.dharma.domain.qa.profile.purpose
  name: Purpose Section Profile
  version: 0.1.0
source: common/tier6/audit/deterministic/section/qa/05-purpose.yaml
section:
  id: purpose
  writing_objective: >-
    Authored placeholder - the rust_dev standard's common/tier6/audit/deterministic/section/qa/05-purpose.yaml
    carries no Writing Guidance for this section; objective pending human author.
    Section behavior is defined by its 3 deterministic audit rule(s) (see validation.rules).
  knowledge_goal: >-
    Authored placeholder - no source exists in the rust_dev standard for this field;
    generated by generate_domain_map_profile.py until a human author writes it.
  reader_goal: >-
    Authored placeholder - no source exists in the rust_dev standard for this field;
    generated by generate_domain_map_profile.py until a human author writes it.
  required_inputs: []
  expected_outputs:
    primary: []
  subsections: {}
  completion:
    checklist:
    - Purpose section exists
    - Purpose states QA intent
    - Purpose defines scope boundaries
  review:
    questions: []
  validation:
    rules:
    - id: qa-sec-purpose-001
      description: Purpose section exists
      condition: document has a section with semantic_type = 'purpose'
      message: Missing optional 'Purpose' section
      severity: warning
      weight: 0.5
      mandatory: false
      evidence:
        type: section_presence
        semantic_type: purpose
    - id: qa-sec-purpose-002
      description: Purpose states QA intent
      condition: section contains a statement of why QA Documentation exists
      message: Purpose section does not state QA intent
      severity: warning
      weight: 0.5
      mandatory: false
      evidence:
        type: content_check
        keywords:
        - quality
        - assurance
        - testing
        - purpose
    - id: qa-sec-purpose-003
      description: Purpose defines scope boundaries
      condition: section defines what QA Documentation is and is not
      message: Purpose section does not define scope boundaries
      severity: warning
      weight: 0.5
      mandatory: false
      evidence:
        type: content_check
        patterns:
        - unlike
        - not
        - does not
