schema:
  id: rust_dev.dharma.domain.qa.profile.integration_testing
  name: Integration Testing Section Profile
  version: 0.1.0
source: >-
  common/tier6/audit/deterministic/section/qa/03-integration_testing.yaml
section:
  id: integration_testing
  writing_objective: >-
    Authored placeholder - the rust_dev standard's common/tier6/audit/deterministic/section/qa/03-integration_testing.yaml
    carries no Writing Guidance for this section; objective pending human author.
    Section behavior is defined by its 4 deterministic audit rule(s) (see validation.rules).
  knowledge_goal: >-
    Authored placeholder - no source exists in the rust_dev standard for this field;
    generated by generate_domain_map_profile.py until a human author writes it.
  reader_goal: >-
    Authored placeholder - no source exists in the rust_dev standard for this field;
    generated by generate_domain_map_profile.py until a human author writes it.
  required_inputs: []
  expected_outputs:
    primary: []
  subsections: {}
  completion:
    checklist:
    - Integration testing section exists
    - Integration testing defines interfaces
    - Integration testing defines scenarios
    - Integration testing references architecture
  review:
    questions: []
  validation:
    rules:
    - id: qa-sec-integration-001
      description: Integration testing section exists
      condition: >-
        document has a section with semantic_type = 'integration_testing'
      message: Missing required 'Integration Testing' section
      severity: error
      weight: 1.5
      mandatory: true
      evidence:
        type: section_presence
        semantic_type: integration_testing
    - id: qa-sec-integration-002
      description: Integration testing defines interfaces
      condition: section specifies which component interfaces are tested
      message: Integration testing section does not define interface scope
      severity: error
      weight: 1.0
      mandatory: true
      evidence:
        type: content_check
        keywords:
        - interface
        - integration
        - component
        - boundary
    - id: qa-sec-integration-003
      description: Integration testing defines scenarios
      condition: >-
        section contains at least one integration test scenario or use case
      message: Integration testing section has no test scenarios
      severity: warning
      weight: 0.5
      mandatory: false
      evidence:
        type: content_check
        patterns:
        - scenario
        - test case
        - flow
        - sequence
    - id: qa-sec-integration-004
      description: Integration testing references architecture
      condition: >-
        section references Architecture Documentation communication paths or data
        flow
      message: >-
        Integration testing does not reference Architecture Documentation
      severity: warning
      weight: 0.5
      mandatory: false
      evidence:
        type: cross_reference
        expected:
        - domain: architecture
          direction: derives_from
