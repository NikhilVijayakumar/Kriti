schema:
  id: rust_dev.dharma.domain.feature.profile.dependencies
  name: Dependencies Section Profile
  version: 0.1.0
source: >-
  common/tier2/audit/deterministic/section/feature/08-dependencies.yaml
section:
  id: dependencies
  writing_objective: >-
    Authored placeholder - the rust_dev standard's common/tier2/audit/deterministic/section/feature/08-dependencies.yaml
    carries no Writing Guidance for this section; objective pending human author.
    Section behavior is defined by its 3 deterministic audit rule(s) (see validation.rules).
  knowledge_goal: >-
    Authored placeholder - no source exists in the rust_dev standard for this field;
    generated by generate_domain_map_profile.py until a human author writes it.
  reader_goal: >-
    Authored placeholder - no source exists in the rust_dev standard for this field;
    generated by generate_domain_map_profile.py until a human author writes it.
  required_inputs: []
  expected_outputs:
    primary: []
  subsections: {}
  completion:
    checklist:
    - Dependencies section exists
    - Dependencies are enumerated
    - Dependencies are not circular
  review:
    questions: []
  validation:
    rules:
    - id: feat-sec-dep-001
      description: Dependencies section exists
      condition: document has a section with semantic_type = 'dependencies'
      message: Missing 'Dependencies' section
      severity: warning
      weight: 0.5
      mandatory: false
      evidence:
        type: section_presence
        semantic_type: dependencies
    - id: feat-sec-dep-002
      description: Dependencies are enumerated
      condition: >-
        section lists each dependency this feature has on other features, systems,
        or standards
      message: Dependencies are not clearly enumerated
      severity: warning
      weight: 0.5
      mandatory: false
      evidence:
        type: content_check
        min_count: 1
    - id: feat-sec-dep-003
      description: Dependencies are not circular
      condition: no dependency chain forms a cycle back to the same feature
      message: Circular dependency detected
      severity: error
      weight: 1.0
      mandatory: true
      evidence:
        type: dependency_cycle
        scope: feature_domain
