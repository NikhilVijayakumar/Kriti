schema:
  id: rust_dev.dharma.domain.feature.profile.inputs
  name: Inputs Section Profile
  version: 0.1.0
source: >-
  common/tier2/audit/deterministic/section/feature/05-inputs.yaml
section:
  id: inputs
  writing_objective: >-
    Authored placeholder - the rust_dev standard's common/tier2/audit/deterministic/section/feature/05-inputs.yaml
    carries no Writing Guidance for this section; objective pending human author.
    Section behavior is defined by its 3 deterministic audit rule(s) (see validation.rules).
  knowledge_goal: >-
    Authored placeholder - no source exists in the rust_dev standard for this field;
    generated by generate_domain_map_profile.py until a human author writes it.
  reader_goal: >-
    Authored placeholder - no source exists in the rust_dev standard for this field;
    generated by generate_domain_map_profile.py until a human author writes it.
  required_inputs: []
  expected_outputs:
    primary: []
  subsections: {}
  completion:
    checklist:
    - Inputs section exists
    - Inputs are enumerated
    - Each input has a source
  review:
    questions: []
  validation:
    rules:
    - id: feat-sec-inputs-001
      description: Inputs section exists
      condition: document has a section with semantic_type = 'inputs'
      message: Missing 'Inputs' section
      severity: warning
      weight: 0.5
      mandatory: false
      evidence:
        type: section_presence
        semantic_type: inputs
    - id: feat-sec-inputs-002
      description: Inputs are enumerated
      condition: section lists each distinct input the feature consumes
      message: Inputs are not clearly enumerated
      severity: warning
      weight: 0.5
      mandatory: false
      evidence:
        type: content_check
        min_count: 1
    - id: feat-sec-inputs-003
      description: Each input has a source
      condition: every listed input identifies its origin or source
      message: Some inputs lack a defined source
      severity: warning
      weight: 0.5
      mandatory: false
      evidence:
        type: content_check
        keywords:
        - from
        - source
        - origin
        - provided by
