schema:
  id: rust_dev.dharma.domain.feature.profile.outputs
  name: Outputs Section Profile
  version: 0.1.0
source: >-
  common/tier2/audit/deterministic/section/feature/06-outputs.yaml
section:
  id: outputs
  writing_objective: >-
    Authored placeholder - the rust_dev standard's common/tier2/audit/deterministic/section/feature/06-outputs.yaml
    carries no Writing Guidance for this section; objective pending human author.
    Section behavior is defined by its 3 deterministic audit rule(s) (see validation.rules).
  knowledge_goal: >-
    Authored placeholder - no source exists in the rust_dev standard for this field;
    generated by generate_domain_map_profile.py until a human author writes it.
  reader_goal: >-
    Authored placeholder - no source exists in the rust_dev standard for this field;
    generated by generate_domain_map_profile.py until a human author writes it.
  required_inputs: []
  expected_outputs:
    primary: []
  subsections: {}
  completion:
    checklist:
    - Outputs section exists
    - Outputs are enumerated
    - Each output has a consumer
  review:
    questions: []
  validation:
    rules:
    - id: feat-sec-outputs-001
      description: Outputs section exists
      condition: document has a section with semantic_type = 'outputs'
      message: Missing 'Outputs' section
      severity: warning
      weight: 0.5
      mandatory: false
      evidence:
        type: section_presence
        semantic_type: outputs
    - id: feat-sec-outputs-002
      description: Outputs are enumerated
      condition: section lists each distinct output the feature produces
      message: Outputs are not clearly enumerated
      severity: warning
      weight: 0.5
      mandatory: false
      evidence:
        type: content_check
        min_count: 1
    - id: feat-sec-outputs-003
      description: Each output has a consumer
      condition: >-
        every listed output identifies its downstream consumer or destination
      message: Some outputs lack a defined consumer
      severity: warning
      weight: 0.5
      mandatory: false
      evidence:
        type: content_check
        keywords:
        - to
        - consumed by
        - destination
        - delivered to
