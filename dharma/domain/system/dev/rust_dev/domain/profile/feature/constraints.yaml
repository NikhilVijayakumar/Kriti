schema:
  id: rust_dev.dharma.domain.feature.profile.constraints
  name: Constraints Section Profile
  version: 0.1.0
source: >-
  common/tier2/audit/deterministic/section/feature/07-constraints.yaml
section:
  id: constraints
  writing_objective: >-
    Authored placeholder - the rust_dev standard's common/tier2/audit/deterministic/section/feature/07-constraints.yaml
    carries no Writing Guidance for this section; objective pending human author.
    Section behavior is defined by its 3 deterministic audit rule(s) (see validation.rules).
  knowledge_goal: >-
    Authored placeholder - no source exists in the rust_dev standard for this field;
    generated by generate_domain_map_profile.py until a human author writes it.
  reader_goal: >-
    Authored placeholder - no source exists in the rust_dev standard for this field;
    generated by generate_domain_map_profile.py until a human author writes it.
  required_inputs: []
  expected_outputs:
    primary: []
  subsections: {}
  completion:
    checklist:
    - Constraints section exists
    - Constraints are clearly stated
    - Constraints trace to philosophy
  review:
    questions: []
  validation:
    rules:
    - id: feat-sec-con-001
      description: Constraints section exists
      condition: document has a section with semantic_type = 'constraints'
      message: Missing 'Constraints' section
      severity: warning
      weight: 0.5
      mandatory: false
      evidence:
        type: section_presence
        semantic_type: constraints
    - id: feat-sec-con-002
      description: Constraints are clearly stated
      condition: each constraint is a discrete, unambiguous restriction
      message: Constraints are ambiguous or unclear
      severity: warning
      weight: 0.5
      mandatory: false
      evidence:
        type: content_check
        patterns:
        - shall not
        - must not
        - limited to
        - maximum
        - minimum
    - id: feat-sec-con-003
      description: Constraints trace to philosophy
      condition: >-
        constraints reference upstream philosophy principles where applicable
      message: Constraints do not trace back to philosophy
      severity: warning
      weight: 0.5
      mandatory: false
      evidence:
        type: cross_reference
        expected:
        - domain: philosophy
          section: guiding_principles
          direction: constrained_by
