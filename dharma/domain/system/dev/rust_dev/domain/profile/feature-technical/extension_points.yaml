schema:
  id: >-
    rust_dev.dharma.domain.feature-technical.profile.extension_points
  name: Extension Points Section Profile
  version: 0.1.0
source: >-
  common/tier3/audit/deterministic/section/feature-technical/16-extension_points.yaml
section:
  id: extension_points
  writing_objective: >-
    Write the Extension Points section, in the imperative voice, in bullet lists form,
    with a technical tone, for the architect. Do: Identify extension points with type
    (plugin, hook, event, configuration) and constraints; reference the Architecture
    plugin model; ensure extension points are architecturally sound. Don't: Name specific
    programming languages, class hierarchies, or library APIs; describe callback implementations;
    define extension mechanisms not present in Architecture.
  knowledge_goal: >-
    Authored placeholder - no source exists in the rust_dev standard for this field;
    generated by generate_domain_map_profile.py until a human author writes it.
  reader_goal: >-
    Authored placeholder - no source exists in the rust_dev standard for this field;
    generated by generate_domain_map_profile.py until a human author writes it.
  required_inputs:
  - Architecture(05) plugin architecture
  - Component Responsibilities
  expected_outputs:
    primary:
    - component diagram showing extension points
  subsections: {}
  completion:
    checklist:
    - Extension Points section exists
    - Extension Points defines extension mechanisms
    - Extension Points is technology-independent
  review:
    questions: []
  validation:
    rules:
    - id: ft-sec-extension-001
      description: Extension Points section exists
      condition: >-
        document has a section with semantic_type = 'extension_points'
      message: Missing optional 'Extension Points' section
      severity: info
      weight: 0.3
      mandatory: false
      evidence:
        type: section_presence
        semantic_type: extension_points
    - id: ft-sec-extension-002
      description: Extension Points defines extension mechanisms
      condition: >-
        section contains descriptions of where and how the feature can be extended
        or customized
      message: >-
        Extension Points section does not define extension mechanisms
      severity: warning
      weight: 0.5
      mandatory: false
      evidence:
        type: content_check
        keywords:
        - extend
        - extension
        - plugin
        - hook
        - customiz
        - override
        - override point
    - id: ft-sec-extension-003
      description: Extension Points is technology-independent
      condition: >-
        section does not reference specific extension frameworks, plugin systems,
        or implementation technologies
      message: >-
        Extension Points section contains implementation technology references
      severity: error
      weight: 1.0
      mandatory: false
      evidence:
        type: keyword_absence
        categories:
        - programming_languages
        - frameworks
        - libraries
