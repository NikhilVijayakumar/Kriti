schema:
  id: rust_dev.dharma.domain.architecture.profile.traceability
  name: Traceability Section Profile
  version: 0.1.0
source: >-
  common/tier2/audit/deterministic/section/architecture/09-traceability.yaml
section:
  id: traceability
  writing_objective: >-
    Authored placeholder - the rust_dev standard's common/tier2/audit/deterministic/section/architecture/09-traceability.yaml
    carries no Writing Guidance for this section; objective pending human author.
    Section behavior is defined by its 4 deterministic audit rule(s) (see validation.rules).
  knowledge_goal: >-
    Authored placeholder - no source exists in the rust_dev standard for this field;
    generated by generate_domain_map_profile.py until a human author writes it.
  reader_goal: >-
    Authored placeholder - no source exists in the rust_dev standard for this field;
    generated by generate_domain_map_profile.py until a human author writes it.
  required_inputs: []
  expected_outputs:
    primary: []
  subsections: {}
  completion:
    checklist:
    - Traceability section exists
    - Derivation chain documented
    - Non-contradiction rule stated
    - Downstream standards listed
  review:
    questions: []
  validation:
    rules:
    - id: arch-sec-trace-001
      description: Traceability section exists
      condition: document has a section with semantic_type = 'traceability'
      message: Missing 'Traceability' section
      severity: warning
      weight: 0.5
      mandatory: false
      evidence:
        type: section_presence
        semantic_type: traceability
    - id: arch-sec-trace-002
      description: Derivation chain documented
      condition: >-
        section includes a diagram or list showing Architecture's position in the
        documentation tier model
      message: Traceability section lacks a derivation chain
      severity: warning
      weight: 0.5
      mandatory: false
      evidence:
        type: content_check
        patterns:
        - Tier
        - derivation
        - chain
    - id: arch-sec-trace-003
      description: Non-contradiction rule stated
      condition: >-
        section explicitly states that no downstream document may contradict Architecture
      message: >-
        Traceability section does not state the non-contradiction rule
      severity: warning
      weight: 0.5
      mandatory: false
      evidence:
        type: content_check
        patterns:
        - non-contradiction
        - contradict
        - must not contradict
    - id: arch-sec-trace-004
      description: Downstream standards listed
      condition: >-
        section lists the standards that Architecture feeds into (Feature Technical,
        Engineering)
      message: Traceability section does not list downstream standards
      severity: warning
      weight: 0.3
      mandatory: false
      evidence:
        type: content_check
        keywords:
        - Feature Technical
        - Engineering
