schema:
  id: rust_dev.dharma.domain.architecture.profile.rationale
  name: Rationale Section Profile
  version: 0.1.0
source: >-
  common/tier2/audit/deterministic/section/architecture/07-rationale.yaml
section:
  id: rationale
  writing_objective: >-
    Authored placeholder - the rust_dev standard's common/tier2/audit/deterministic/section/architecture/07-rationale.yaml
    carries no Writing Guidance for this section; objective pending human author.
    Section behavior is defined by its 4 deterministic audit rule(s) (see validation.rules).
  knowledge_goal: >-
    Authored placeholder - no source exists in the rust_dev standard for this field;
    generated by generate_domain_map_profile.py until a human author writes it.
  reader_goal: >-
    Authored placeholder - no source exists in the rust_dev standard for this field;
    generated by generate_domain_map_profile.py until a human author writes it.
  required_inputs: []
  expected_outputs:
    primary: []
  subsections: {}
  completion:
    checklist:
    - Rationale section exists
    - Each decision has alternatives documented
    - Each decision has a rejection reason
    - Rationale references architectural goals
  review:
    questions: []
  validation:
    rules:
    - id: arch-sec-rat-001
      description: Rationale section exists
      condition: document has a section with semantic_type = 'rationale'
      message: Missing 'Rationale' section
      severity: warning
      weight: 0.5
      mandatory: false
      evidence:
        type: section_presence
        semantic_type: rationale
    - id: arch-sec-rat-002
      description: Each decision has alternatives documented
      condition: >-
        every rationale entry names at least one alternative that was considered
      message: One or more rationale entries lack documented alternatives
      severity: warning
      weight: 0.5
      mandatory: false
      evidence:
        type: field_presence
        per_entry:
          field: Alternatives Considered
    - id: arch-sec-rat-003
      description: Each decision has a rejection reason
      condition: >-
        every rationale entry explains why alternatives were rejected
      message: One or more rationale entries lack rejection reasons
      severity: warning
      weight: 0.5
      mandatory: false
      evidence:
        type: field_presence
        per_entry:
          field: Rejection Reason
    - id: arch-sec-rat-004
      description: Rationale references architectural goals
      condition: each decision ties back to an architectural goal or pillar
      message: Rationale entries are not tied to architectural goals
      severity: warning
      weight: 0.3
      mandatory: false
      evidence:
        type: field_presence
        per_entry:
          field: Architectural Goal
