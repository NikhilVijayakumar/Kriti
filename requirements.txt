# Kriti repo dependencies
# Added by pcems_2026 review-pipeline proposal — Phase 3 (readability scoring)
textstat>=0.7.3
# Added by pcems_2026 render-paper-and-reference-grounding proposal —
# bundles a real pandoc binary via pip, no system-level install needed
pypandoc-binary>=1.13
# Build-time only — regenerates reference/template/*_reference.docx
# (script/render/generate_reference_docx.py) if the template spec changes
python-docx>=1.2.0
